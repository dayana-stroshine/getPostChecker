<h1>GET Request Received</h1>
<table>
    <thead>
        <tr>
            <th colspan="2">Request Data</th>
        </tr>
    </thead>
    <tbody>
      {{#each dataList}}
      <tr>
        <td>{{this.name}}</td>
        <td>{{this.value}}</td>
      </tr>
      {{/each}}
    </tbody>
</table>